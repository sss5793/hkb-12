const HkbHistDTO = require('./hkbHistDTO');
const CategoryDTO = require('./categoryDTO');
const PaymentDTO = require('./paymentDTO');
const UserDTO = require('./userDTO');

module.exports = {
  HkbHistDTO,
  CategoryDTO,
  PaymentDTO,
  UserDTO,
};
